{
  "name": "manisha-subedi-portfolio",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "npm run build && node --test tests/site.test.mjs"
  },
  "dependencies": {
    "@astrojs/mdx": "^8.0.0",
    "astro": "^7.2.10"
  }
}
